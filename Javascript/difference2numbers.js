// Write a program to find the difference between two numbers.

function difference2numbers(a, b) {
  return a - b;
}

const number1 = 5;
const number2 = 10;

const result = difference2numbers(number1, number2);

console.log(`The difference between ${number1} and ${number2} is ${result}`);