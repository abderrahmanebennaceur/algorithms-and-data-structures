// Write a program to find the quotient of two numbers.

function quotient2numbers(a, b) {
  return a / b;
}

const number1 = 5;
const number2 = 10;

const result = quotient2numbers(number1, number2);

console.log(`The quotient of ${number1} and ${number2} is ${result}`);